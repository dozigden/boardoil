# Block dependency lifecycle scripts during installs; explicit npm run commands still work.
ignore-scripts=true
# Apply the cooldown when resolving updates; npm ci restores the reviewed lockfile.
min-release-age=7
# Older npm versions may silently ignore the release-age policy.
engine-strict=true
